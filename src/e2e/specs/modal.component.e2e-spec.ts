import { browser, element, by } from 'protractor';

describe('Modal', () => {
  /*beforeEach(async () => {
    return await browser.get('/modal');
  });
  it('should have phone number fields pre filled', () => {
    expect(element(by.css('mm-modal tel watel')).getText()).toEqual('tel');
  });
  it("should have submit button be disabled initially", () => {
    browser.get("/modal");
    let submitButton = element(
          by.css("submit input[type=submit]"));

    expect(submitButton.isEnabled()).toEqual(false);
  });
  it("should only enable submit button when mobile number field is filled and terms checkbox is checked", () => {
    browser.get("/modal");
    let submitButton = element(
          by.css("input[type=submit]"));
    expect(submitButton.isEnabled()).toEqual(true);
  });*/
});
